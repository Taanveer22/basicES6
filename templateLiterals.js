let a = 10;
let b = 20;
let sum = 'the sum of ' + a + ' and ' + b + ' is '+ (a + b);
console.log(sum);

let add = `the add of ${a} and ${b} is ${a + b}`;
console.log(add);

const numbers = [33, 44, 34, 65];
const student = { name: 'john', age: 30 };
const math = `the math of ${numbers[0]} and ${student.age} is ${numbers[0] + student.age}`;
console.log(math);
